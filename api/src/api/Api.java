package api;

import java.util.Scanner;

public class Api {
	/* API(Application Programming Interface)  == 라이브러리
	 * 프로그램 개발에 자주 사용되는 클래스 및 인터페이스 모음
	 * 
	 * 자바 API 
	 * String 클래스는 import 구문 생성 안됨.
	 * java.lang : 기본 패키지
	 * java.lang에 있는 클래스를 호출시엔 import구문이 실행되지 않음
	 * 다른 클래스를 사용할 때는 모여있는 장소를 지정해줘야함.
	 * 
	 * Class Object : Class Object is the root of the class hierarchy.
	 * 				: 가장 높은 곳에 있는 클래스  == 단군
	 */
	
	String str = ""; //import 구문 생성 불가
	Scanner sc = new Scanner(System.in); //import 구문 생성

}
